<UserControl x:Class="IntifaceGameHapticsRouter.ModControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:IntifaceGameHapticsRouter"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="#FFE5E5E5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Button Grid.Row="0" Name="AttachButton" IsEnabled="false" Content="Attach To Process" Margin="10,5,0,0" HorizontalAlignment="Left" Click="AttachButton_Click" />
        <Label Grid.Row="0" Name="ErrorLabel" Content="Select Process To Inject"  HorizontalAlignment="Center" VerticalAlignment="Center"/>
        <Button Grid.Row="0" Name="RefreshButton" Content="Refresh List" Margin="0,5,10,0" HorizontalAlignment="Right" Click="RefreshButton_Click" />
        <ListBox Grid.Row="1" Name="ProcessListBox" Margin="10,10,10,10" />
    </Grid>
</UserControl>
